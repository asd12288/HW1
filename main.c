#include <stdio.h>
#include <stdlib.h>
#include <string.h>




void duplicate_string(char *str) {

    char *copy;
    int len;


    len = strlen(str);
    copy = maloc(sizeof(char) * len);


    strcpy()

}




int main() {

  
    
    original = "hello my name is ilan";

    len = strlen(original + 1);

    copy = maloc(sizeof(char) * len);


    return 0;
}